using Framework.Attributes;

namespace Framework.TriggerArea
{
    public enum StandardSprites
    {
        [StringValue("Square")] BOX,
        [StringValue("Circle")] CIRCLE,
        [StringValue("Capsule")] CAPSULE
    }
}